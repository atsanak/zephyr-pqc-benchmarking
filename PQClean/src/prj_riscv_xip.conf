# RISC-V XIP configuration for qemu_riscv32_xip
# Handles eXecute-In-Place memory constraints and linker requirements

# Basic memory settings optimized for XIP
CONFIG_MAIN_STACK_SIZE=8192
CONFIG_HEAP_MEM_POOL_SIZE=16384

# XIP-specific configurations
CONFIG_XIP=y
CONFIG_BUILD_NO_GAP_FILL=y
CONFIG_LINKER_ORPHAN_SECTION_PLACE=y

# Enable random subsystem
CONFIG_ENTROPY_GENERATOR=y
CONFIG_TEST_RANDOM_GENERATOR=y

# Enable console output
CONFIG_CONSOLE=y
CONFIG_PRINTK=y
CONFIG_UART_CONSOLE=y

# Enable timing for benchmarks
CONFIG_TIMING_FUNCTIONS=y

# Use minimal libc for memory constraints
CONFIG_MINIMAL_LIBC=y

# Basic subsystems
CONFIG_KERNEL_MEM_POOL=y

# QEMU target configuration
CONFIG_QEMU_TARGET=y

# Disable problematic features
CONFIG_WARN_DEPRECATED=n
CONFIG_DEBUG=n
CONFIG_DEBUG_INFO=n
CONFIG_HAS_CMSIS_CORE=n
CONFIG_HAS_NRFX=n
CONFIG_SOC_FAMILY_NORDIC_NRF=n
CONFIG_QEMU_ICOUNT=n
